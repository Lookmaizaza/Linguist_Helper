นี่คือไฟล์ **README.md** ที่เขียนขึ้นอย่างละเอียด เพื่อให้นำไปแนบกับโปรเจกต์ส่งงานได้โดยตรง  
ครอบคลุมสิ่งที่ต้องเตรียม การติดตั้ง และวิธีการใช้งานจริงตาม `app.py`

---

### วิธีการสร้างไฟล์

1. สร้างไฟล์ชื่อ `README.md` แล้ววางเนื้อหาด้านล่างลงไป
2. สร้างไฟล์ชื่อ `requirements.txt` แล้วใส่รายชื่อ Library (ดูในหัวข้อการติดตั้ง)

---

# 🧩 Linguist Helper Tool

**เครื่องมือช่วยดึงบทความและตัดคำภาษาไทย สำหรับงานวิจัยทางภาษา**

โปรแกรมนี้พัฒนาด้วย **Streamlit** เพื่อช่วยดึงเนื้อหาบทความจากเว็บไซต์ข่าวออนไลน์  
ทำความสะอาดข้อความ (Clean Text) ตัดคำภาษาไทย (Tokenization)  
และส่งออกผลลัพธ์เป็นไฟล์ **Excel** และ **Text** เพื่อนำไปวิเคราะห์ต่อได้ทันที

---

## ✨ ฟีเจอร์หลัก (Key Features)

- **Universal Web Extraction**  
  ระบบดึงเนื้อหาหลักจากเว็บข่าวอัตโนมัติ  
  (รองรับเว็บข่าวไทยทั่วไป เช่น Thairath, Sanook ฯลฯ)

- **Text Cleaning & Normalization**
  - ลบ script, nav, footer, header และ tag ที่ไม่เกี่ยวข้อง
  - ลบอักขระซ่อน (zero-width characters)
  - แปลง HTML entities และจัดรูปแบบข้อความให้สะอาด

- **Thai Tokenization**
  - ใช้ Library **Attacut** เป็นหลัก
  - หากไม่พบ Attacut จะ fallback เป็น **Deepcut**
  - หากไม่มีทั้งสอง จะใช้การตัดคำแบบพื้นฐานแทน

- **Automatic Statistics**
  - จำนวนคำทั้งหมด (Total Tokens)
  - จำนวนคำไม่ซ้ำ (Unique Tokens)

- **Detailed Export**
  - **Excel (.xlsx)**
    - Summary
    - Tokens (1 คำต่อ 1 แถว)
    - Original Text
  - **Text (.txt)**
    - รายงานผลพร้อมข้อความต้นฉบับและการตัดคำ

- **User Friendly**
  - ใช้งานผ่าน Web Interface
  - ไม่ต้องเขียนโค้ด
  - เหมาะสำหรับงานวิจัยและการเรียนการสอน

---

## 🛠️ การติดตั้ง (Installation Guide)

### 1. ติดตั้ง Python

ตรวจสอบให้แน่ใจว่าเครื่องติดตั้ง **Python 3.8 ขึ้นไป**  
ดาวน์โหลดได้ที่: https://www.python.org/downloads/

---

### 2. เตรียมไฟล์ requirements.txt

สร้างไฟล์ชื่อ `requirements.txt` แล้วใส่เนื้อหาดังนี้

```text
streamlit
pandas
requests
beautifulsoup4
openpyxl
attacut
deepcut
```

### 3. ติดตั้ง Library

เปิด **Command Prompt** (Terminal) ในโฟลเดอร์งาน แล้วพิมพ์คำสั่ง:

```bash
pip install -r requirements.txt

```

*รอจนกว่าการติดตั้งจะเสร็จสมบูรณ์*

---

## 🚀 วิธีการใช้งาน (User Guide)

### 1. เริ่มต้นโปรแกรม

พิมพ์คำสั่งต่อไปนี้ใน Terminal เพื่อเปิดหน้าเว็บแอปพลิเคชัน:

```bash
streamlit run app.py

```

*(สมมติว่าไฟล์โค้ดของคุณชื่อ `app.py`)*
โปรแกรมจะเปิดหน้าต่างเว็บเบราว์เซอร์ขึ้นมาอัตโนมัติ

### 2. การนำเข้าข้อมูล (Input)

* **ช่อง (URL):** วางลิงก์บทความข่าวที่ต้องการวิเคราะห์ (สามารถวางได้หลายบรรทัด หลายลิงก์พร้อมกัน)

### 3. เริ่มประมวลผล

กดปุ่ม **🔍 ดึงข้อมูลและตัดคำ**

* โปรแกรมจะทำการดึงข้อมูลพร้อมกันหลายเว็บ (Parallel Processing)
* ระบบจะกรอง "คำขยะ" และ "เมนู" ออกให้
* ระบบจะตัดคำไทยด้วย Attacut

### 4. ตรวจสอบและดาวน์โหลด (Review & Download)

* ดูตาราง **Data Preview** ด้านล่างเพื่อตรวจสอบความถูกต้อง
* เลือกดาวน์โหลดไฟล์ตามต้องการ:
* 📥 **Excel (.xlsx):** แนะนำสำหรับนำไปทำ Pivot Table หรือดูข้อมูลแยกชีต
* 📥 **Text (.txt):** แนะนำสำหรับการนำไปใช้ในโปรแกรมสถิติอื่นๆ



---

## 📂 โครงสร้างไฟล์ผลลัพธ์ (Output Format)

เมื่อดาวน์โหลดไฟล์ **Excel (.xlsx)** ระบบจะได้ไฟล์ที่ประกอบด้วย **3 ชีต (Sheets)** ดังนี้

### 1. **Sheet: Summary** (สรุปภาพรวม)

ใช้แสดงข้อมูลสถิติพื้นฐานของบทความที่วิเคราะห์

- `URL` : แหล่งที่มาของบทความ  
- `Word Count` : จำนวนคำทั้งหมดหลังการตัดคำ  
- `Unique Words` : จำนวนคำที่ไม่ซ้ำ  
- `Generated` : วันที่และเวลาที่สร้างรายงาน  

---

### 2. **Sheet: Tokens** (ผลลัพธ์การตัดคำ)

แสดงรายการคำที่ได้จากการตัดคำภาษาไทย โดยเรียงตามลำดับที่ปรากฏในบทความ

- `Word` : คำศัพท์ (Token)  
- `Index` : ลำดับของคำในข้อความ  

---

### 3. **Sheet: Original Text** (ข้อความต้นฉบับ)

แสดงข้อความบทความต้นฉบับที่ผ่านการทำความสะอาดแล้ว

- `Original Text` : เนื้อหาบทความหลังจากลบ tag และจัดรูปแบบเรียบร้อย  

---

> **หมายเหตุ**
> - ไม่มีการระบุ Method (Sniper / AI / Sweep) ในไฟล์ผลลัพธ์  
> - ไม่มี Sheet `Word_List` และไม่มี `Tokenized Text` แบบคั่น `|` ในไฟล์ Excel  
> - การคั่นคำด้วย `|` จะแสดงในหน้าเว็บและไฟล์ `.txt` เท่านั้น


---

## ❓ การแก้ไขปัญหาเบื้องต้น (Troubleshooting)

* **Error: Module not found...**
* แปลว่ายังติดตั้ง Library ไม่ครบ ให้รันคำสั่ง `pip install -r requirements.txt` อีกครั้ง


* **ภาษาต่างดาวใน Excel (CSV)**
* โปรแกรมนี้ใช้มาตรฐาน `utf-8-sig` แล้ว หากเปิดแล้วยังเพี้ยน ให้ลองเปิด Excel > Data > Get Data > From Text/CSV แล้วเลือก Origin เป็น `65001: Unicode (UTF-8)`



---


**Developed for Lexical Analysis Course | Powered by Streamlit & Attacut**ห
