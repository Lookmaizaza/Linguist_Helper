นี่คือไฟล์ **`README.md`** ที่เขียนขึ้นอย่างละเอียด เพื่อให้คุณ (หรือ "ครูแพรว") สามารถนำไปแนบกับโปรเจกต์ส่งงานได้เลยครับ โดยครอบคลุมทั้งสิ่งที่ต้องเตรียม การติดตั้ง และคู่มือการใช้งานครับ

---

### วิธีการสร้างไฟล์

1. สร้างไฟล์ชื่อ `README.md` แล้ววางเนื้อหาด้านล่างลงไป
2. สร้างไฟล์ชื่อ `requirements.txt` แล้ววางรายชื่อ Library ลงไป (มีระบุไว้ในขั้นตอนที่ 2)

---

# 🧩 The Linguist's Assistant (Master Edition)

**เครื่องมือช่วยดึงบทความและตัดคำภาษาไทยสำหรับนักภาษาศาสตร์**

โปรแกรมนี้ถูกพัฒนาขึ้นเพื่อช่วยให้นักภาษาศาสตร์สามารถรวบรวมข้อมูล (Corpus) จากเว็บไซต์ข่าวออนไลน์ ทำความสะอาดข้อมูล (Clean Data) ตัดคำภาษาไทย (Tokenization) และส่งออกเป็นไฟล์ Excel/CSV เพื่อนำไปวิเคราะห์ต่อได้ทันที โดยไม่ต้องเขียนโค้ดเอง

## ✨ ฟีเจอร์หลัก (Key Features)

* **3-Layer Extraction:** ระบบดึงข้อมูลอัจฉริยะ 3 ขั้นตอน (เจาะจง ID > AI วิเคราะห์ > กวาดทั้งหมด) มั่นใจได้ว่าดึงเนื้อหาข่าวได้ครบถ้วน
* **Ultra Clean Mode:** ตัดส่วนที่ไม่เกี่ยวข้องออกอัตโนมัติ เช่น เมนูนำทาง (Navigation), โฆษณา, ข่าวหวย/ดวง และลิงก์แนะนำข่าว
* **Thai Tokenization:** ตัดคำภาษาไทยแม่นยำด้วย Library **Attacut**
* **Detailed Export:**
* **Excel (.xlsx):** แยกชีตสรุป (Summary) และชีตรายการคำละเอียด (Word List)
* **CSV (.csv):** รองรับภาษาไทยสมบูรณ์ (UTF-8-SIG) เปิดใน Excel ได้ทันที ไม่เป็นภาษายึกยือ


* **User Friendly:** ใช้งานง่ายผ่านหน้าเว็บ (Web Interface) รองรับทั้งการวาง URL และวางข้อความดิบ

---

## 🛠️ การติดตั้ง (Installation Guide)

### 1. ติดตั้ง Python

ตรวจสอบให้แน่ใจว่าเครื่องคอมพิวเตอร์ของคุณติดตั้ง **Python 3.8** ขึ้นไปแล้ว
(ดาวน์โหลดได้ที่ [python.org](https://www.python.org/downloads/))

### 2. เตรียมไฟล์ dependencies

สร้างไฟล์ชื่อ `requirements.txt` ในโฟลเดอร์เดียวกับโค้ดโปรแกรม และวางเนื้อหาต่อไปนี้ลงไป:

```text
streamlit
pandas
requests
beautifulsoup4
trafilatura
attacut
openpyxl

```

### 3. ติดตั้ง Library

เปิด **Command Prompt** (Terminal) ในโฟลเดอร์งาน แล้วพิมพ์คำสั่ง:

```bash
pip install -r requirements.txt

```

*รอจนกว่าการติดตั้งจะเสร็จสมบูรณ์*

---

## 🚀 วิธีการใช้งาน (User Guide)

### 1. เริ่มต้นโปรแกรม

พิมพ์คำสั่งต่อไปนี้ใน Terminal เพื่อเปิดหน้าเว็บแอปพลิเคชัน:

```bash
streamlit run app.py

```

*(สมมติว่าไฟล์โค้ดของคุณชื่อ `app.py`)*
โปรแกรมจะเปิดหน้าต่างเว็บเบราว์เซอร์ขึ้นมาอัตโนมัติ

### 2. การนำเข้าข้อมูล (Input)

คุณสามารถเลือกใส่ข้อมูลได้ 2 ช่องทาง:

* **ช่องที่ 1 (URL):** วางลิงก์บทความข่าวที่ต้องการวิเคราะห์ (สามารถวางได้หลายบรรทัด หลายลิงก์พร้อมกัน)
* **ช่องที่ 2 (Paste Text):** กรณีที่ลิงก์ดึงไม่ได้ หรือมีข้อความอยู่แล้ว สามารถก๊อปปี้ข้อความมาวางใส่ช่องนี้ได้เลย

### 3. เริ่มประมวลผล

กดปุ่ม **"🚀 รันการวิเคราะห์ (Full Option)"**

* โปรแกรมจะทำการดึงข้อมูลพร้อมกันหลายเว็บ (Parallel Processing)
* ระบบจะกรอง "คำขยะ" และ "เมนู" ออกให้
* ระบบจะตัดคำไทยด้วย Attacut

### 4. ตรวจสอบและดาวน์โหลด (Review & Download)

* ดูตาราง **Data Preview** ด้านล่างเพื่อตรวจสอบความถูกต้อง
* เลือกดาวน์โหลดไฟล์ตามต้องการ:
* 📥 **Excel (.xlsx):** แนะนำสำหรับนำไปทำ Pivot Table หรือดูข้อมูลแยกชีต
* 📥 **CSV (.csv):** แนะนำสำหรับการนำไปใช้ในโปรแกรมสถิติอื่นๆ



---

## 📂 โครงสร้างไฟล์ผลลัพธ์ (Output Format)

เมื่อดาวน์โหลดไฟล์ **Excel** คุณจะได้ไฟล์ที่มี 2 ชีต (Sheets):

1. **Sheet: Summary** (สรุปภาพรวม)
* `Source`: แหล่งที่มา (URL)
* `Method`: วิธีที่ใช้ดึงข้อมูล (Sniper/AI/Sweep)
* `Raw`: เนื้อหาข่าวต้นฉบับที่คลีนแล้ว
* `Tokenized Text`: เนื้อหาที่ตัดคำแล้ว คั่นด้วยเครื่องหมาย `|`
* `Word Count`: จำนวนคำทั้งหมด


2. **Sheet: Word_List** (รายการคำละเอียด - 1 คำต่อ 1 แถว)
* `Source`: แหล่งที่มา
* `Word_Index`: ลำดับที่ของคำในประโยค
* `Word`: คำศัพท์ (Token)



---

## ❓ การแก้ไขปัญหาเบื้องต้น (Troubleshooting)

* **Error: Module not found...**
* แปลว่ายังติดตั้ง Library ไม่ครบ ให้รันคำสั่ง `pip install -r requirements.txt` อีกครั้ง


* **ภาษาต่างดาวใน Excel (CSV)**
* โปรแกรมนี้ใช้มาตรฐาน `utf-8-sig` แล้ว หากเปิดแล้วยังเพี้ยน ให้ลองเปิด Excel > Data > Get Data > From Text/CSV แล้วเลือก Origin เป็น `65001: Unicode (UTF-8)`



---

**Developed for Lexical Analysis Course | Powered by Streamlit & Attacut**ห